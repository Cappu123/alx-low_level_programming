#include <stdio.h>

/**
 * main - Entry poinnt
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	const char *s = "\"Programming is like building a multilingual puzzle\n";

	puts(s);
	return (0);
}
