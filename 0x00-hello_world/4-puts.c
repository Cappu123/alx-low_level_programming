#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(const char *s)
{
	*s = "Programming is like building a multilingual puzzle";
	puts(s);
	puts(\n);
	return (0);
}
