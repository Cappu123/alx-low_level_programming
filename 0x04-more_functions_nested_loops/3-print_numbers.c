#include "main.h"

/*This function prints the numbers from 0 to 9,
 * followed by a new line
 * 
 */

void print_numbers(void)
{
	int a;
	if (a >= 0 && a<=9)
	{
		_putchar("a")
		_putchar("\n");
	}
	return (0);
}
