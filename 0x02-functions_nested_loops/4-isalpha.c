#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	int r;
	for (r = 65; r <= 90; r++) || for (r = 97; r <= 122; r++)
	{
		_putchar(1);
	}
	{
		_putchar(0);
	}
	return;
}
